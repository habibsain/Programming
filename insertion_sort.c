#include <stdio.h>

void sort(int* arr, int num)
{

}

void print_arr(int* arr, int num)
{
    for (int i = 0; i < num; i++)
    {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

int main()
{
    int num = 5
    int arr[num] = {2, 4, 1, 5, 3}
    //print array
    print_arr(arr, num);
    //sort(arr, num)

    //print sorted array
}